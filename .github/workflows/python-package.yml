name: DataMov CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Python
      uses: MatteoH2O1999/setup-python@v2
      with:
        python-version: 2.7.18

    - name: Run tests
      run: |
        pytest
